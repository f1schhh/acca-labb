import { SiteConfig } from "../types";

const siteConfig: SiteConfig = {
  siteName: "ACCA",
  description:
    "ACCA is a platform for logging and keeping track of your job applications.",
  author: "ACCA",
  keywords: ["job applications", "ACCA", "platform", "logging"],
  baseUrl: "https://www.accaplatform.com",
};

export default siteConfig;
